# Pins the Gradle daemon JVM to 21 so builds work regardless of the user's system Java.
# Combined with the foojay-resolver-convention plugin in settings.gradle.kts, Gradle will
# auto-download JDK 21 if it isn't installed locally.
toolchainVersion=21
